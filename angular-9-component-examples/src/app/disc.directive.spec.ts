import { DiscDirective } from './disc.directive';

describe('DiscDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscDirective();
    expect(directive).toBeTruthy();
  });
});
